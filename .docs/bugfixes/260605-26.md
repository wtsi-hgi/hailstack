- [x] A freshly created cluster reaches `cluster ready`, but `spark-history-server.service` fails on the master because Spark falls back to `file:/tmp/spark-events` and that directory does not exist. The live master has no `/opt/spark/conf/spark-defaults.conf`, even though Hailstack intends to use `/home/ubuntu/data/spark-history`; Hailstack should configure/create the Spark history event-log directory before starting the service so it is active without manual repair.
  - Linked Hailstack's managed `/etc/spark/conf/spark-defaults.conf` into Spark's runtime `/opt/spark/conf/spark-defaults.conf` path for masters and workers.
  - Preserved creation of `/home/ubuntu/data/spark-history` before `spark-history-server` starts and added a regression asserting both the config link and history directory are prepared first.
  - Implementor and reviewer ran `ruff`, `pyright`, targeted cloud-init tests, and the full pytest suite; full suite passed with `372 passed, 8 skipped`.
