- [x] Remote build-image now gets past packages.sh, hadoop.sh, Hail, and Jupyter, but ubuntu/gnomad.sh fails because it tries to install gnomad==3.0.4 and uv reports there is no such Python package version. GNOMAD_VERSION should be handled according to what it actually represents so gnomAD provisioning completes without user involvement.
  - Confirmed PyPI publishes the `gnomad` Python methods package on a separate `0.x` release line and has no `3.0.4` release; `3.0.4` remains the gnomAD data release carried by the compatibility bundle.
  - Updated `packer/scripts/ubuntu/gnomad.sh` to install `gnomad==0.8.2` by default via `GNOMAD_METHODS_VERSION`, while preserving `GNOMAD_VERSION` as the data-release value in the provisioning verification output.
  - Added a hermetic Packer-script regression that fails if the data release is passed to `uv` as the Python package pin.
  - Passed Ruff, Pyright, targeted Packer/developer-doc tests, and the full pytest suite.
