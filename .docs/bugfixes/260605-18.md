- [x] `hailstack create --dry-run` with the user's Ceph S3 backend works with Pulumi CLI 3.226.0 but fails with the currently installed Pulumi CLI 3.245.0. Real remote evidence: with correct S3 credentials and normalized endpoint `https://cog.sanger.ac.uk`, Pulumi 3.245.0 fails `PutObject` for `.pulumi/meta.yaml` with `XAmzContentSHA256Mismatch`; Pulumi 3.226.0, 3.200.0, 3.150.0, and 3.100.0 log into the same backend successfully. Hailstack's Apptainer build currently runs `curl -fsSL https://get.pulumi.com | sh`, so it installs latest, and README venv instructions only say `pulumi version` without a known-good version. Hailstack should not install/recommend an unpinned Pulumi CLI that is incompatible with this Ceph RGW, and backend failures with `XAmzContentSHA256Mismatch` should give an actionable hint to use the supported Pulumi CLI version.
  - Added `SUPPORTED_PULUMI_CLI_VERSION = "3.226.0"` in `src/hailstack/tool_versions.py`, used by Pulumi backend checksum diagnostics and by the Apptainer build-time install pin.
  - Updated `Apptainer.def` to install Pulumi through `get.pulumi.com` with `--version "${PULUMI_VERSION}"`, and updated README venv/troubleshooting guidance to use Pulumi CLI `3.226.0` and warn about checksum mismatches from newer Pulumi versions.
  - Added regression tests in `tests/test_apptainer_definition.py` and `tests/pulumi/test_stack.py` for the Apptainer pin and `XAmzContentSHA256Mismatch` hint while preserving endpoint normalization from `3dd0b7f`.
  - Ran `timeout 120 uv run ruff check src/ tests/`, `timeout 120 uv run ruff format src/ tests/`, `timeout 180 uv run pyright`, `timeout 180 uv run pytest tests/pulumi/test_stack.py tests/test_apptainer_definition.py -v`, and `timeout 600 uv run pytest tests/ -v`.
