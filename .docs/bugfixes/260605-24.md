- [x] Real cluster nodes become ACTIVE but SSH with the private key matching the
  third configured public key fails because Nova keypairs only seed
  `ssh_keys.public_keys[0]` early, while Hailstack writes all configured keys in
  the delayed final-stage shellscript. Hailstack should make every configured
  SSH public key available during the cloud-init config stage for the configured
  login user, while preserving the existing shell setup script behavior.
  - TDD: added failing master/worker cloud-init regressions that required a
    `text/cloud-config` MIME part with all configured keys plus the existing
    `text/x-shellscript` setup payload; confirmed both failed against the prior
    single-shellscript renderer.
  - Touched `src/hailstack/pulumi/cloud_init.py`,
    `tests/pulumi/test_cloud_init.py`, and `tests/pulumi/test_resources.py`.
  - Rendered master and worker user-data as deterministic MIME multipart:
    cloud-config first installs all `ssh_keys.public_keys` for
    `cluster.ssh_username`, then the existing Hailstack shell setup script runs
    as a `text/x-shellscript` final-stage part.
  - Preserved the existing shellscript authorized_keys write and did not change
    Nova keypair resource selection.
  - Ran `timeout 120 uv run ruff check src/ tests/`,
    `timeout 120 uv run ruff format src/ tests/`,
    `timeout 180 uv run pyright`,
    `timeout 180 uv run pytest tests/pulumi/test_cloud_init.py tests/pulumi/test_resources.py -v`,
    and `timeout 600 uv run pytest tests/ -v`.
