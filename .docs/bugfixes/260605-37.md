- [x] After fixing detailed-status resource parsing in commit `98f66ea`, `hailstack status --config ~/hailstack/my-cluster.toml --dotenv ~/hailstack/.env --detailed` succeeds for the master but reports every worker service and resource as `unreachable`. Manual SSH to each worker succeeds when using an explicit `ProxyCommand` that carries the same host-key, batch, identity, and timeout options through the master, while Hailstack uses `ssh -J ubuntu@<master> ...` and the implicit jump connection fails host-key verification. Hailstack should build worker SSH probes with an explicit ProxyCommand (or otherwise propagate the same safe SSH options to the jump connection) so detailed status reaches workers through the master without manual SSH setup.
  - Added a regression proving jump-host probes use an explicit `ProxyCommand` instead of bare `-J`, and that the nested jump SSH command carries batch, timeout, host-key, and explicit identity options.
  - Reused the same SSH probe options for direct probes and jump proxy commands, preserving the previous remote-command quoting fix for complex status probes.
  - Implementor and reviewer ran `ruff`, `pyright`, and the full pytest suite; full suite passed with `387 passed, 8 skipped`.
