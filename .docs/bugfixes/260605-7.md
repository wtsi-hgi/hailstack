- [x] Remote build-image on the user's OpenStack project launches the Packer source server on cloudforms_network, logs `Packer floating IP pool: none configured`, then Packer repeatedly tries SSH to the fixed address `192.168.252.82` and gets `dial tcp 192.168.252.82:22: connect: no route to host`. Hailstack should make the routing/floating-IP requirement work correctly or fail quickly with a clear, actionable message instead of waiting for the 30 minute SSH timeout.
  - Updated `src/hailstack/packer/builder.py` so the default Packer runner captures live output, enables a temporary debug log when the user has not configured one, stops Packer early on SSH `no route to host`, and raises an actionable `PackerError`.
  - Added regression coverage in `tests/packer/test_builder.py` for early debug-log detection and for the fixed-IP routing error message that points to `cluster.floating_ip_pool`, `[packer].floating_ip_pool`, or running from a host that routes to `cluster.network_name`.
