- [x] The explicit-port Packer fix creates the management port successfully, but fails before Packer when creating the Lustre port with `--no-security-group --disable-port-security`. The user's OpenStack policy rejects explicitly setting `port_security_enabled`: `ForbiddenException: 403 ... (rule:create_port and rule:create_port:port_security_enabled) is disallowed by policy`. Hailstack should avoid requesting forbidden port-security policy changes while still keeping the temporary SSH security group off the Lustre port.
  - Removed `--disable-port-security` from temporary Packer Lustre port creation, leaving `--no-security-group` so the temporary SSH security group stays off that interface without requesting a forbidden `port_security_enabled` policy change.
  - Adjusted the Packer builder regression to assert the Lustre port command includes `--no-security-group` and omits `--disable-port-security`; verified it failed before the implementation and passed after.
  - Updated the README build-image networking wording to describe temporary Lustre ports as created without security groups, not with port security disabled.
  - Ran `timeout 120 uv run ruff check src/ tests/`, `timeout 120 uv run ruff format src/ tests/`, `timeout 180 uv run pyright`, `timeout 180 uv run pytest tests/packer/test_builder.py -v`, `timeout 180 uv run pytest tests/test_developer_docs.py -v`, and `timeout 600 uv run pytest tests/ -v`.
  - Local Packer CLI is unavailable, so `test_e2_packer_template_validates_with_packer_cli` skipped with `packer CLI not installed`.
