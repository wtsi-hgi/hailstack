- [x] Packer reaches SSH on the build instance but fails authentication with `ssh: unable to authenticate, attempted methods [none publickey], no supported methods remain`; Hailstack should seed usable user SSH keys so image builds work without manual keypair setup and users can manually SSH to created nodes later.
  - Added regression coverage for the checked-in OpenStack Packer source using `config_drive = true`, all Pulumi master/worker instances using `config_drive = true`, and docs naming `~/.ssh/id_ed25519.pub` as the normal public key source for `ssh_keys.public_keys`.
  - Enabled Nova config drive for Packer image-build instances and cluster instances without baking cluster SSH keys into the reusable image.
  - Verified targeted regressions failed before the fix and passed after it.
  - Ran `timeout 120 uv run ruff check src/ tests/`, `timeout 120 uv run ruff format src/ tests/`, `timeout 180 uv run pyright`, `timeout 300 uv run pytest tests/packer/ tests/pulumi/ tests/cli/commands/test_build_image.py -v`, and `timeout 600 uv run pytest tests/ -v`.
  - Local Packer CLI is unavailable, so `test_e2_packer_template_validates_with_packer_cli` skipped with `packer CLI not installed`.
