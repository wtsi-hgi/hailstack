- [x] Packer still reports `Bad script 'scripts/base.sh': stat scripts/base.sh: no such file or directory` from an installed README-style venv run; the Packer template itself must resolve provisioner scripts from its own directory so `hailstack ...` works without `cd` into installed `_data/packer`.
  - Rooted every shell provisioner script in `packer/hailstack.pkr.hcl` at `${path.root}` so Packer resolves installed `_data/packer/scripts/...` independently of process cwd.
  - Added a regression test that rejects bare cwd-relative `scripts/...` entries in the checked-in Packer template.
  - Implementor and reviewer passed `ruff`, `pyright`, targeted Packer tests, and the full pytest suite; local Packer CLI validation remained skipped because `packer` is not on PATH here.
