- [x] With the Ceph state secret corrected, `hailstack create --config ~/hailstack/my-cluster2.toml --dotenv ~/hailstack/.env --dry-run` reaches `pre-flight passed` but Pulumi preview fails with `PulumiError: Network '84eb7e1b-0c70-46da-ab28-c0098dcd6e50' not found` because the configured `cluster.lustre_network` is already a network UUID and Hailstack tries to look it up as a name.
  - Added `tests/pulumi/test_resources.py::test_lustre_network_uuid_is_used_without_provider_lookup`, which fails when UUID-valued Lustre networks are sent to Pulumi's `getNetwork` data source and now asserts the Lustre ports receive the UUID directly.
  - Updated `src/hailstack/pulumi/resources.py` so UUID-shaped OpenStack network values pass through the Pulumi resource layer while friendly network names still resolve through `get_network(name=...)`.
  - Preserved the existing Lustre port behavior: Lustre ports still use `no_security_groups=True`, omit security groups and port-security overrides, and blank optional Lustre network values remain unset.
  - Ran `timeout 180 uv run pytest tests/ -v -k test_lustre_network_uuid_is_used_without_provider_lookup` before and after the fix, `timeout 180 uv run pytest tests/pulumi/test_resources.py -v`, `timeout 180 uv run pytest tests/integration/test_pulumi_lifecycle.py -v`, `timeout 120 uv run ruff check --fix src/ tests/`, `timeout 120 uv run ruff format src/ tests/`, `timeout 120 uv run ruff check src/ tests/`, `timeout 180 uv run pyright`, and `timeout 600 uv run pytest tests/ -v`.
