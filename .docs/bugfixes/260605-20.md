- [x] On the user's machine, after backend/login and preflight issues are fixed, `hailstack create --config ~/hailstack/my-cluster.toml --dotenv ~/hailstack/.env --dry-run` reaches `pre-flight passed` and then fails for a first-time stack preview with Pulumi Automation error: `could not create secrets manager for new stack: passphrase must be set with PULUMI_CONFIG_PASSPHRASE or PULUMI_CONFIG_PASSPHRASE_FILE environment variables`. This dry-run path uses `_preview_new_stack` with an ephemeral `file://` backend and `allow_missing_runtime_secrets=True`; users following README should not need to set Pulumi secrets passphrase just to preview a new cluster.
  - Touched `src/hailstack/pulumi/stack.py` and `tests/pulumi/test_stack.py`.
  - Added regression coverage proving first-time ephemeral previews get a non-empty Pulumi passphrase when the caller provides neither `PULUMI_CONFIG_PASSPHRASE` nor `PULUMI_CONFIG_PASSPHRASE_FILE`, while preserving both explicit caller-provided forms.
  - Scoped the default passphrase to `_preview_new_stack`'s temporary `file://` backend by adding a preview-specific env helper; persisted stack operations still use `_pulumi_env` without a default passphrase.
