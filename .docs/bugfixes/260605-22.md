- [x] With backend/passphrase issues worked around, real `hailstack create` starts Pulumi apply but fails creating `openstack_compute_keypair_v2 re3hailstacktest-keypair`. Nova returns 400: `Invalid input for field/attribute keypair... Additional properties are not allowed ('cluster_name', 'tags' were unexpected)`. The Pulumi keypair resource currently passes `value_specs=_cluster_value_specs(cluster_name)`, which expands to `cluster_name` and `tags`. This is not accepted by the user's Nova API. Hailstack should create keypairs without unsupported extra properties while preserving cluster tagging/metadata on resources where OpenStack supports it.
  - Touched `src/hailstack/pulumi/resources.py` and `tests/pulumi/test_resources.py`.
  - Removed `value_specs` from the Nova keypair resource so keypairs are created with only `name` and `public_key`.
  - Updated the Pulumi resource regression to assert keypair inputs omit `value_specs`, `cluster_name`, and `tags`; existing security group, port, instance, volume, and floating IP tagging/metadata paths remain unchanged.
