- [x] Rebuilding the image with the pinned Jupyter stack reaches `packer/scripts/ubuntu/jupyter.sh`, but that script fails at `pip check` because the Jupyter install upgrades packages required by Hail/gnomAD: `decorator` becomes `5.3.1` while `hail 0.2.137` requires `decorator<5`, `python-json-logger` becomes `4.1.0` while Hail requires `<3,>=2.0.2`, and `ipython` becomes `9.14.1` while `biocommons-seqrepo 0.6.11` requires `ipython~=8.4`. Hailstack should pin the Jupyter-compatible dependency repair tightly enough that `pip check` passes after Hail, gnomAD, and Jupyter are installed.
  - Added Jupyter-step repair pins for `decorator==4.4.2`, `ipython==8.39.0`, and `python-json-logger==2.0.7` in the same `uv pip install --upgrade` transaction as the Jupyter pins.
  - Kept the real `pip check` plus Jupyter import/version validation and extended that validation to assert the repaired shared dependency versions.
  - Extended the Packer script regression to start from the observed bad dependency-upgrade state and require the script to repair Hail and gnomAD constraints before validation.
  - Implementor and reviewer ran `bash -n`, targeted Jupyter/Packer tests, `ruff`, `pyright`, and the full pytest suite; full suite passed with `376 passed, 8 skipped`.
