- [x] The explicit-port Packer fix creates both a management port and a Lustre port and passes both IDs to Packer as `ports`. On the user's OpenStack project, Packer then created a floating IP but attempted to associate it with the Lustre port ID (`1af6b613-...`) instead of the management/cloudforms port, failing with `Error associating floating IP ... with instance port ...: Resource not found`. Hailstack must ensure Packer's floating IP association targets only the management port while still attaching the configured Lustre network.
  - Added regressions for floating-IP plus Lustre builds passing only the management port in `ports`, preserving `lustre_network`, attaching Lustre via HCL `networks`, and avoiding temporary Lustre port commands.
  - Removed temporary Lustre port creation so floating-IP builds create and clean up only the explicit management port; no-floating-IP builds keep the existing `networks` path.
  - Added the missing Packer OpenStack `instance_floating_ip_net = var.network` setting so floating-IP association targets the management/cloudforms network on multi-homed build instances, plus a checked-in template regression for that contract.
  - Updated README wording that described floating-IP Lustre ports.
  - Ran `timeout 120 uv run ruff check src/ tests/`, `timeout 120 uv run ruff format src/ tests/`, `timeout 180 uv run pyright`, `timeout 180 uv run pytest tests/packer/test_builder.py tests/integration/test_packer_scripts.py -v`, `timeout 180 uv run pytest tests/test_developer_docs.py -v`, and `timeout 600 uv run pytest tests/ -v`.
  - Local Packer CLI remains unavailable, so `test_e2_packer_template_validates_with_packer_cli` skipped with `packer CLI not installed`.
