- [x] `hailstack create` for a first-time persisted stack reached `pre-flight passed` and `creating infrastructure`, then failed while creating/selecting the Pulumi stack because Pulumi required `PULUMI_CONFIG_PASSPHRASE` or `PULUMI_CONFIG_PASSPHRASE_FILE`. The earlier dry-run fix only covered the ephemeral preview backend, but README-style first-time creates also need noninteractive stack-encryption setup.
  - Touched `src/hailstack/pulumi/stack.py`, `tests/pulumi/test_stack.py`, and `README.md`.
  - Added regression coverage for first-time persisted `up()` env setup, shared `_pulumi_env()` defaults, and preservation of caller-provided `PULUMI_CONFIG_PASSPHRASE` and `PULUMI_CONFIG_PASSPHRASE_FILE`.
  - Removed the preview-only passphrase helper so previews and persisted stack operations share one Pulumi environment path.
  - Used `config.ceph_s3.secret_key` as the default passphrase source because it is already required for the state bucket and is stable across persisted Pulumi operations. The value is not printed or documented.
