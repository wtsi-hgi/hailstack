- [x] README-style venv installs require running `hailstack build-image` from the installed Packer data directory because Packer cannot find relative provisioner scripts from the normal README workflow; `hailstack ...` should work after `cd ~/hailstack`, `source .venv/bin/activate`, and `source openrc.sh`.
  - Ran Packer from the resolved template directory while still passing the absolute template path, so installed `_data/packer/scripts/...` paths resolve from any caller working directory.
  - Added a regression test covering an installed-style Packer data directory with a separate caller cwd.
  - Implementor and reviewer passed `ruff`, `pyright`, targeted Packer tests, and the full pytest suite.
