- [x] After Packer provisioners started running as root, `base.sh` succeeded and the build reached `packer/scripts/ubuntu/packages.sh`, but that script failed with `E: Could not get lock /var/lib/dpkg/lock-frontend. It is held by process 4719 (unattended-upgr)` and `E: Unable to acquire the dpkg frontend lock`. Hailstack image builds should wait for or neutralize unattended apt/dpkg activity before running apt commands so unattended-upgrades cannot randomly break Packer provisioning.
  - Added a reusable Packer apt/dpkg helper that stops unattended apt units for the build boot, waits with a bounded timeout on known lock files, and wraps `apt-get` with `DPkg::Lock::Timeout`.
  - Uploaded the helper before shell provisioning and routed `base.sh`, `ubuntu/packages.sh`, and `ubuntu/netdata.sh` through it before apt operations.
  - Added regressions for helper upload, required asset validation, bounded lock handling, and apt script wrapper usage.
  - Ran `timeout 120 uv run ruff check src/ tests/`, `timeout 120 uv run ruff format src/ tests/`, `timeout 180 uv run pyright`, `timeout 180 uv run pytest tests/integration/test_packer_scripts.py tests/packer/test_builder.py -v`, `timeout 180 uv run pytest tests/test_developer_docs.py -v`, and `timeout 600 uv run pytest tests/ -v`.
  - Local Packer CLI is unavailable, so `test_e2_packer_template_validates_with_packer_cli` skipped with `packer CLI not installed`.
