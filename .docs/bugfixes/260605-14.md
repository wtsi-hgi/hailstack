- [x] hailstack build-image now reaches SSH and starts Packer provisioning, but ubuntu/packages.sh exits with status 1 after installing packages and printing Java/Python versions. The real user-machine Packer log ended with: openjdk version "11.0.31"; Python 3.12.13; then Packer reported Remote command exited with '1' for sudo -E /tmp/script_9040.sh and no artifact was created. The image build should complete the package provisioning step instead of failing there.
  - Added a hermetic regression for `packer/scripts/ubuntu/packages.sh` that simulates Ubuntu's older generic `scala` package and requires the configured `SCALA_VERSION` deb to be downloaded and installed instead.
  - Updated `packages.sh` to stop installing Ubuntu's generic `scala` package, install `scala-${SCALA_VERSION}.deb` through the lock-aware apt helper, and report clearer Java/Python/Scala version-check failures.
  - Ran `timeout 120 uv run ruff check src/ tests/`, `timeout 120 uv run ruff format src/ tests/`, `timeout 180 uv run pyright`, `timeout 180 uv run pytest tests/packer/test_builder.py tests/integration/test_packer_scripts.py -v`, `timeout 180 uv run pytest tests/test_developer_docs.py -v`, and `timeout 600 uv run pytest tests/ -v`.
  - Local Packer CLI remains unavailable, so `test_e2_packer_template_validates_with_packer_cli` skipped with `packer CLI not installed`; the full suite passed with existing Pulumi OpenStack deprecation warnings.
