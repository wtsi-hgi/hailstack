- [x] `hailstack create --dry-run` on the user's README-style config fails Pulumi backend login before preview. The config uses documented bare Ceph endpoint `ceph_s3.endpoint = "cog.sanger.ac.uk"`; Hailstack passes `s3://hailstack-state?endpoint=cog.sanger.ac.uk` to Pulumi and only sets AWS credentials. Pulumi v3.245.0 first reports `Invalid region: region was not a valid DNS name`; when AWS_REGION is set manually it tries a scheme-less URL like `//hailstack-state./cog.sanger.ac.uk/...`; when the endpoint is manually changed to `https://cog.sanger.ac.uk` and AWS_REGION/AWS_DEFAULT_REGION are `us-east-1`, Pulumi reaches the backend and returns a real 403 credential/access error instead. Hailstack should normalize bare Ceph S3 endpoints for Pulumi the same way rollout uploads do, set a valid default S3 region for Pulumi backend access unless the caller already set one, and surface real backend auth/access errors.
  - Touched `src/hailstack/pulumi/stack.py` and `tests/pulumi/test_stack.py`.
  - Added Pulumi login regression coverage for bare Ceph endpoint normalization and S3 region env defaults, then normalized backend endpoint URLs and populated `AWS_REGION`/`AWS_DEFAULT_REGION` without overriding caller-provided values.
