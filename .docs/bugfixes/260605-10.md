- [x] The temporary Packer SSH security-group fix passes `security_groups = ["default", <temporary-ssh-sg>]` to the OpenStack builder while also passing both `cloudforms_network` and `lustre-hgi01` in `networks`. On the user's OpenStack project, two consecutive builds then left the Packer source server stuck in Nova `BUILD/spawning` with no addresses. Earlier evidence showed `openstack server add security group ... cloudforms_ssh_in` on an already-active multi-network VM complained that a network required `port_security_enabled`, while a no-extra-security-group Packer VM became ACTIVE. Hailstack should apply the temporary SSH security group only to the management/cloudforms interface, not server-wide across the Lustre interface.
  - Floating-IP `build-image` now creates temporary explicit Neutron ports, with `default` plus the SSH-open temporary security group only on the management port and any Lustre port using `--no-security-group --disable-port-security`.
  - Packer receives ordered `ports` with the management port first; blank/non-floating-IP builds still use the previous `networks` path with config-drive, network UUID resolution, Lustre attachment, floating-pool fallback, Packer cwd, log-path handling, and no-route diagnostics preserved.
  - Temporary ports are deleted before the temporary SSH security group; setup failures clean up partial resources before Packer starts and cleanup warnings do not mask the primary result.
  - Ran `timeout 120 uv run ruff check src/ tests/`, `timeout 120 uv run ruff format src/ tests/`, `timeout 180 uv run pyright`, `timeout 180 uv run pytest tests/packer/test_builder.py -v`, `timeout 180 uv run pytest tests/test_developer_docs.py -v`, and `timeout 600 uv run pytest tests/ -v`.
  - Local Packer CLI is unavailable, so `test_e2_packer_template_validates_with_packer_cli` skipped with `packer CLI not installed`.
