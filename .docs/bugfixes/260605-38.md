- [x] On the live cluster created from commit `275aaea`, `hailstack status --detailed` reaches all nodes, but `spark-worker.service` fails on multiple workers after repeated `Connection refused` attempts to `master:7077`. The Spark master process is active, but `ss -ltnp` shows it listening on `[::ffff:127.0.1.1]:7077` because Hailstack writes the master node's `/etc/hosts` with `127.0.1.1 master <cluster>-master`; workers correctly resolve `master` to the master's private IP. Hailstack should render the master node's own `/etc/hosts` so the `master` alias points at the master private IP, matching worker resolution, so Spark master binds a worker-reachable address and all workers stay connected.
  - Added direct cloud-init and Pulumi create-flow regressions proving the master user-data maps `master` to the resolved master private IP and does not map that alias to `127.0.1.1`.
  - Threaded Pulumi's resolved `master_private_ip` output into master cloud-init rendering while keeping the direct renderer compatible when no master IP is supplied.
  - Preserved worker host mappings and prior image, network/Lustre, boot, Hadoop config, and SSH detailed-status regressions.
  - Implementor and reviewer ran `ruff`, `pyright`, and the full pytest suite; full suite passed with `389 passed, 8 skipped`.
