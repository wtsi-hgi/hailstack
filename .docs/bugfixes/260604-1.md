- [x] Fix needless first-use timeout failures across build-image and related lifecycle waits, starting from the reported `hailstack build-image --config ~/hailstack/my-cluster.toml --dotenv ~/hailstack/.env` Packer OpenStack `Timeout waiting for SSH.` after about 5 minutes 19 seconds; extend slow OpenStack readiness waits where appropriate and make failures expose useful logs/diagnostics beyond raw machine-readable output while preserving image ID parsing.
  - Added a 30 minute Packer OpenStack SSH readiness timeout and regression coverage in `tests/packer/test_builder.py`.
  - Rendered readable Packer failure diagnostics while preserving raw machine-readable output for detailed troubleshooting.
  - Extended slow lifecycle SSH waits for reboot recovery and install-time Ansible connection retries without lengthening quick status probes.
  - Reviewer passed the code review; full documented `uv run ...` quality gates were blocked because `uv` is not installed on PATH in this workspace.
