- [x] When `build-image` uses `cluster.floating_ip_pool = "public"`, the Packer source VM boots and `sshd` starts, but Packer repeatedly logs `dial tcp 172.27.26.171:22: i/o timeout`. `openstack server show` shows the source VM only has the `default` security group, and `openstack security group rule list default` only permits ingress from the same default security group. Manually adding the existing SSH-open `cloudforms_ssh_in` security group made Packer connect immediately. Hailstack should make build-image attach an SSH-open build security group automatically when it uses a floating IP, instead of requiring users to know or manually attach a site-specific security group.
  - Added floating-IP build regressions for temporary SSH security-group creation, Packer var wiring, success/failure cleanup, cleanup warnings, and quota/permission failures before Packer starts.
  - `build-image` now creates `hailstack-packer-ssh-*` with TCP/22 ingress when the effective Packer floating IP pool is non-blank, passes it to Packer with `default`, and deletes it afterward.
  - Updated Packer HCL and README build-image networking docs; preserved config-drive, network UUID resolution, Lustre attachment, floating-IP fallback, Packer cwd, `PACKER_LOG_PATH`, and no-route diagnostics coverage.
  - Ran `timeout 120 uv run ruff check src/ tests/`, `timeout 120 uv run ruff format src/ tests/`, `timeout 180 uv run pyright`, `timeout 180 uv run pytest tests/packer/test_builder.py -v`, `timeout 180 uv run pytest tests/test_developer_docs.py -v`, and `timeout 600 uv run pytest tests/ -v`.
  - Local Packer CLI is unavailable, so `test_e2_packer_template_validates_with_packer_cli` skipped with `packer CLI not installed`.
- [ ] After the SSH security group was added manually, Packer connected and began provisioning, but `packer/scripts/base.sh` failed immediately with `E: Could not open lock file /var/lib/apt/lists/lock - open (13: Permission denied)` and `E: Unable to lock directory /var/lib/apt/lists/`. Hailstack's Packer shell provisioners are running as the SSH user instead of root, so package installation cannot work.
