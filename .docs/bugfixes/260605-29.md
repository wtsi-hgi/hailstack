- [x] After a successful rebuild, `hailstack create --config ~/hailstack/my-cluster.toml --dotenv ~/hailstack/.env` fails during OpenStack preflight because more than one active Glance image exists with the name `hailstack-hail-0.2.137-gnomad-3.0.4-r2`. Users following the README can naturally run `hailstack build-image` more than once, so Hailstack should make cluster creation deterministic after rebuilds instead of failing with `More than one Image exists with the name ...`.
  - Added duplicate-image regressions for OpenStack image lookup, create-command propagation, and Pulumi instance rendering.
  - `create` now resolves active images by name via `openstack image list --name ... --status active --long`, selects the newest matching image deterministically, and passes that image ID through the Pulumi stack runner.
  - Pulumi master and worker instances now use the resolved `image_id` when available, avoiding ambiguous `image_name` boot requests after repeated `build-image` runs.
  - Implementor and reviewer ran `ruff`, `pyright`, and the full pytest suite; full suite passed with `379 passed, 8 skipped`.
