- [x] A fresh cluster created from commit `480adf4` still prints `cluster ready` before cloud-init final setup completes because the baked image enables cloud-init OS updates in `/etc/cloud/cloud.cfg.d/50_sanger.cfg` with `package_update: true` and `package_upgrade: true`. The live master is running `apt-get ... dist-upgrade`, rebuilding Lustre DKMS modules for a newer kernel, and leaving `spark-history-server.service` inactive while `cloud-final.service` is still running several minutes after `hailstack create` returned. Hailstack should explicitly override baked cloud-init package update/upgrade defaults in its user-data so first boot does Hailstack's deterministic configuration instead of unbounded OS upgrades and DKMS rebuilds.
  - Added a cloud-init regression for master and worker user-data explicitly setting `package_update: false`, `package_upgrade: false`, and `package_reboot_if_required: false`.
  - The shared cloud-config payload now overrides baked image package-update defaults while preserving early bootcmd cleanup, SSH authorized keys, and configured Lustre fstab behavior.
  - Implementor and reviewer ran `ruff`, `pyright`, targeted cloud-init tests, and the full pytest suite; full suite passed with `383 passed, 8 skipped`.
