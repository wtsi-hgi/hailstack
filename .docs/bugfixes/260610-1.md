- [x] The image has built successfully, but on create (either with or without the dry run flag) it has another error: `S3Error: Unable to access Ceph S3 backend at cog.sanger.ac.uk ... api error SignatureDoesNotMatch: UnknownError`.
  - Added a Pulumi backend regression test for `SignatureDoesNotMatch` from `pulumi login` against the configured Ceph S3 backend.
  - Extended the existing supported Pulumi CLI hint so known Ceph backend checksum and signature mismatches both preserve the raw S3 error while pointing users to Pulumi CLI `3.226.0`.
  - Updated README troubleshooting guidance to mention `SignatureDoesNotMatch` alongside `XAmzContentSHA256Mismatch`.
