- [x] A fresh cluster created from commit `672b7f0` completes cloud-init and mounts the configured Lustre target, but `hdfs-namenode.service` fails. The live master wrote generated Hadoop XML under `/etc/hadoop/conf`, while `/opt/hadoop/bin/hdfs` and the systemd services use `/opt/hadoop-3.4.1/etc/hadoop`; `hdfs namenode -format` therefore formats `/tmp/hadoop-root/dfs/name` and the service starts without the generated `dfs.namenode.name.dir` configuration. Hailstack should configure Hadoop in the directory used by the installed Hadoop distribution, or make all generated Hadoop commands and services use the same generated config, so `hailstack create` produces a cluster with a running HDFS NameNode instead of a failed service.
  - Added a cloud-init regression proving generated Hadoop XML is written before `hdfs namenode -format` and `hdfs-namenode` restart, and that the old `/etc/hadoop/conf` path is no longer used.
  - Changed the generated Hadoop configuration directory to `/opt/hadoop/etc/hadoop`, matching the installed Hadoop distribution and the `/opt/hadoop/bin/hdfs` commands used by baked systemd services.
  - Updated Pulumi/user-data assertions to expect the active Hadoop config path while preserving prior duplicate-image, network/Lustre, baked-service, package-update, and bounded-mount regressions.
  - Implementor and reviewer ran `ruff`, `pyright`, and the full pytest suite; full suite passed with `385 passed, 8 skipped`.
