- [x] After disabling baked cloud-init package upgrades, first boot no longer runs `apt-get dist-upgrade`, but `cloud-final.service` still blocks in Hailstack's own shellscript at `mount /lustre`. The live master process tree shows `/usr/bin/cloud-init modules --mode=final` -> `bash /var/lib/cloud/instance/scripts/part-002` -> `mount /lustre` running for several minutes, so Spark History and nginx final configuration remain incomplete after `hailstack create` prints `cluster ready`. The same boot also still logs `Failed to mask unit: File /etc/systemd/system/mountLustre.service already exists` even though Hailstack now uses `systemctl mask --force`. Hailstack should make configured Lustre mounting bounded/non-blocking enough that a slow or unavailable Lustre target cannot hold cloud-final indefinitely, and baked service masking should handle local unit files robustly.
  - Added a cloud-init regression proving the configured Lustre mount is attempted with a bounded timeout and that local baked service unit files are removed before masking.
  - Replaced the unbounded `mount /lustre` with a logged `timeout --kill-after=15s 120s mount /lustre` attempt that lets cloud-final continue if the Lustre target is slow or unavailable.
  - Baked service cleanup now removes `/etc/systemd/system/<service>` and reloads systemd before force-masking, avoiding the observed local-unit mask failure.
  - Implementor and reviewer ran `ruff`, `pyright`, targeted cloud-init tests, and the full pytest suite; full suite passed with `384 passed, 8 skipped`.
