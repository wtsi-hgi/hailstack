- [x] After the configured-Lustre cleanup fix, a fresh `hailstack create` completes and every node becomes ACTIVE, but cloud-init still never reaches final setup. The new master shows the configured Lustre fstab entry first and `mountLustre.service` is inactive, but `/var/log/cloud-init-output.log` contains `systemctl: unrecognized option '--kill-whom=all'` and `Failed to mask unit: File /etc/systemd/system/mountLustre.service already exists`, and `systemctl list-jobs` shows `cloud-final.service` waiting on baked `metricbeat-openstack-setup.service`. That service is running `/usr/local/sbin/metricbeat-setup` with a child `curl -s 169.254.169.254/openstack/2018-08-27/meta_data.json` and no timeout. Hailstack should make first-boot cleanup use systemd-compatible commands and prevent baked site metricbeat setup from blocking cloud-init/final Hailstack setup, so `create` produces a actually-ready cluster instead of printing `cluster ready` while Jupyter/Spark/nginx final configuration is still waiting.
  - Added a cloud-init regression for systemd-compatible baked-service cleanup: no unsupported `--kill-whom`, forced masks for existing unit files, and early metricbeat setup cleanup.
  - First-boot `bootcmd` now stops, disables, and force-masks both baked `mountLustre.service` and `metricbeat-openstack-setup.service` before they can block `cloud-final`.
  - Preserved the configured-Lustre cleanup from `260605-31`: stale baked Lustre fstab rows are removed/commented and only `cluster.lustre_mount_target` is made active.
  - Implementor and reviewer ran `ruff`, `pyright`, targeted cloud-init tests, and the full pytest suite; full suite passed with `382 passed, 8 skipped`.
