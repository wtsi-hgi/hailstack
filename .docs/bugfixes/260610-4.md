- [x] Remote verification showed Pulumi 3.245.0 can pass backend login and dry-run preview with checksum compatibility settings, but real first-time create still fails creating the persisted stack lock with `XAmzContentSHA256Mismatch` on `.pulumi/locks/...json`; Hailstack should require or select the known-good Pulumi 3.226.0 for this Ceph RGW backend and document why latest Pulumi is not safe here.
  - Updated `src/hailstack/pulumi/stack.py` so Pulumi CLI resolution still checks `PATH` and `~/.pulumi/bin/pulumi` deterministically, but prefers a discovered `3.226.0` candidate over a newer one. `_ResolvedPulumiCommand.version` still receives a parsed `semver.VersionInfo`, and unsupported versions are rejected before preview/up/destroy Automation commands run.
  - Backend checks now reject a selected non-`3.226.0` Pulumi after successful login, before persisted Automation create/up can start. The diagnostic names the selected path/version, explains that this Ceph RGW-backed state path requires Pulumi `3.226.0` for persisted stack writes, and includes `curl -fsSL https://get.pulumi.com | sh -s -- --version 3.226.0`.
  - Kept the checksum compatibility env defaults, but changed `XAmzContentSHA256Mismatch` guidance: newer Pulumi points at the required `3.226.0` install, while `3.226.0` preserves the raw S3 error and tells the user the backend is still rejecting state writes and needs site administrator attention. `SignatureDoesNotMatch` under `3.226.0` still points at bad `ceph_s3.access_key`, `ceph_s3.secret_key`, or `--dotenv` values.
  - Updated `README.md` so the old latest-Pulumi install command is explicitly not sufficient for this backend, while noting latest Pulumi is normally desirable elsewhere. Updated `.docs/bugfixes/260610-3.md` to mark its latest-Pulumi conclusion as superseded by this create-path verification.
  - Added/updated regressions in `tests/pulumi/test_stack.py` for rejecting Pulumi `3.245.0` after successful backend login without reaching Automation create/up, preferring a home `3.226.0` binary over a newer PATH binary, preserving `_ResolvedPulumiCommand.version` as non-`None`, preserving checksum env defaults, and splitting checksum/signature diagnostics by Pulumi version.
  - Follow-up verification on `ubuntu@172.27.19.102` fast-forwarded `~/hailstack` from `63cc759` to `745cc4d`, reinstalled Hailstack into `~/hailstack/.venv`, and confirmed the selected Pulumi CLI was `/home/ubuntu/.pulumi/bin/pulumi v3.226.0`. `hailstack create --config ~/hailstack/my-cluster2.toml --dotenv ~/hailstack/.env --dry-run` passed and planned 35 resources. A real `hailstack create --config ~/hailstack/my-cluster2.toml --dotenv ~/hailstack/.env` then completed with `cluster ready` for `re3hailstacktest2` and master IP `172.27.24.118`. Cleanup used the same config with `hailstack destroy --config ~/hailstack/my-cluster2.toml --dotenv ~/hailstack/.env`, confirmed `re3hailstacktest2`, and completed with `Cluster 're3hailstacktest2' destroyed.` A scoped post-cleanup OpenStack name check found no remaining `re3hailstacktest2` servers, ports, security groups, or keypairs.
  - Ran failing-first `timeout --foreground 180 uv run pytest tests/pulumi/test_stack.py -v -k "known_good_home or rejects_newer or checksum_mismatch or signature_mismatch"` before implementation (5 failed, 1 passed), then reran it after implementation (6 passed). Final checks run: `timeout --foreground 180 uv run pytest tests/pulumi/test_stack.py -v`, `timeout --foreground 120 uv run ruff check --fix src/ tests/` (first reported one wrapped assertion, then passed), `timeout --foreground 120 uv run ruff format src/ tests/`, `timeout --foreground 180 uv run pyright`, `timeout --foreground 120 uv run ruff check src/ tests/`, and `timeout --foreground 600 uv run pytest tests/ -v` (409 passed, 8 skipped).
