- [x] Superseded by `.docs/bugfixes/260610-4.md`: the user reports create still fails with `S3Error: Unable to access Ceph S3 backend at cog.sanger.ac.uk ... SignatureDoesNotMatch ...` after earlier Pulumi CLI selection work. Follow-up remote testing at this point showed Pulumi CLI `3.245.0` can log in to the Ceph backend when Hailstack uses correct Ceph credentials and sets AWS SDK checksum compatibility variables: `AWS_REQUEST_CHECKSUM_CALCULATION=when_required` and `AWS_RESPONSE_CHECKSUM_VALIDATION=when_required`. Later real create-path verification disproved the conclusion that latest Pulumi is safe: Pulumi `3.245.0` can pass login and dry-run preview, then fail persisted stack lock writes with `XAmzContentSHA256Mismatch`.
  - Added regression tests proving Hailstack defaults the AWS checksum compatibility variables without overriding caller values, keeps `~/.pulumi/bin/pulumi` discoverable when `PATH` misses it, rejects empty or unparseable `pulumi version` output before Automation receives `version=None`, and rejects CLIs older than Pulumi Automation API's `3.1.0` minimum. The temporary tests and docs that allowed Pulumi CLI `3.245.0` after successful backend login were corrected in `.docs/bugfixes/260610-4.md`.
  - Preserved the resolved-command behavior: each `AutomationStackRunner` caches one selected Pulumi executable, backend login and Automation API operations use the same executable, and real `auto.Stack` preview, preview-destroy, and destroy calls receive a `_ResolvedPulumiCommand.version` parsed as `semver.VersionInfo`.
  - Updated backend diagnostics to preserve raw S3 errors while reporting the selected Pulumi CLI path/version and checksum environment values. `SignatureDoesNotMatch` now points users at `ceph_s3.access_key`, `ceph_s3.secret_key`, and `--dotenv` values when checksum compatibility is active; the later `260610-4` fix changed persistent `XAmzContentSHA256Mismatch` guidance to require known-good Pulumi CLI `3.226.0` for this backend.
  - Updated README installation/troubleshooting docs at the time to describe checksum compatibility for newer Pulumi/AWS SDK clients. The later `260610-4` fix corrected those docs to say the standard latest-Pulumi installer is not sufficient for this Ceph RGW create path.
  - Ran failing-first `timeout --foreground 180 uv run pytest tests/pulumi/test_stack.py -v -k "checksum_compatibility or explicit_checksum or allows_newer or checksum_mismatch or signature_mismatch or prefers_path"` and `timeout --foreground 180 uv run pytest tests/pulumi/test_stack.py -v -k "too_old_cli or checksum_compatibility or allows_newer"`, then final `timeout --foreground 180 uv run pytest tests/pulumi/test_stack.py -v -k unparseable_cli_version`, `timeout --foreground 180 uv run pytest tests/pulumi/test_stack.py -v`, `timeout --foreground 180 uv run pytest tests/integration/test_pulumi_lifecycle.py -v`, `timeout --foreground 180 uv run pytest tests/test_apptainer_definition.py -v`, `timeout --foreground 120 uv run ruff check --fix src/ tests/`, `timeout --foreground 120 uv run ruff format src/ tests/`, `timeout --foreground 180 uv run pyright`, `timeout --foreground 600 uv run pytest tests/ -v`, and `timeout --foreground 120 uv run ruff check src/ tests/`.
