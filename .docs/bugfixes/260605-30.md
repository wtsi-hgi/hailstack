- [x] The duplicate-image create fix in `.docs/bugfixes/260605-29.md` removed the `More than one Image exists with the name ...` failure, but on the user's OpenStack CLI `openstack image list --name ... --status active --long -f json` does not include `Created At`, `created_at`, `Updated At`, or `updated_at`. Live verification selected the older image `e49f3d46-54e4-4ec2-9241-1c560bcaf5c3` instead of the rebuilt image `19d4a6df-7435-4734-991a-c1629506659e`, even though `openstack image show <id> -f json` exposes `created_at` and `updated_at`. Hailstack should deterministically select the newest active matching image using available detailed timestamps, so repeated README-style `build-image` runs are followed by `create` booting the latest image.
  - Added a regression for duplicate active image-list rows that omit timestamp fields while per-ID `image show` details expose `created_at` and `updated_at`.
  - Enriched duplicate image candidates by querying `openstack image show <id> -f json` only when needed, avoiding ambiguous `image show <name>` while preserving timestamp-rich list-row behavior.
  - Image selection now sorts by detailed creation/update timestamps with a deterministic ID fallback, so create chooses the rebuilt image after repeated `build-image` runs.
  - Implementor and reviewer ran `ruff`, `pyright`, targeted create-image tests, and the full pytest suite; full suite passed with `380 passed, 8 skipped`.
