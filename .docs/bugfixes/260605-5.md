- [x] Packer fails with `Bad networks format: network uuid is not in proper format (cloudforms_network)` during `build-image`; if Packer needs a UUID, Hailstack should either document that clearly or resolve the configured network name to the UUID itself.
  - Added pre-Packer resolution of `cluster.network_name` to an OpenStack network UUID for Packer, while preserving UUID pass-through configs.
  - Added clear `PackerError` failures that name the unresolved network and suggest `openstack network list`/credential checks before Packer launches.
  - Updated README docs to state that `cluster.network_name` is a friendly network name and Hailstack resolves it to the UUID Packer requires.
