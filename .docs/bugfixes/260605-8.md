- [x] With `cluster.lustre_network = "lustre-hgi01"` configured, remote `build-image` still launches the Packer source VM with only `cloudforms_network`. After adding `cluster.floating_ip_pool = "public"`, Packer allocates floating IP `172.27.23.130`, but SSH never opens; the instance console is stuck in systemd `A start job is running for Wait for Network to be Configured (... / no limit)`. `openstack server show` for the Packer VM shows only `cloudforms_network` addresses and no `lustre-hgi01`, while working predecessor clusters have both `cloudforms_network=192.168...` and `lustre-hgi01=10.177...`. Hailstack should use the configured Lustre network for Packer build instances too, so base images that expect the Lustre interface can boot and be SSH-provisioned.
  - Added builder regressions in `tests/packer/test_builder.py` for resolving configured Lustre network names to UUIDs, passing UUID configs through, ignoring blank values, and failing before Packer launch when lookup fails.
  - Updated `src/hailstack/packer/builder.py` and `packer/hailstack.pkr.hcl` so Packer always receives the management network and receives an optional resolved `lustre_network` var; the template attaches one or two networks accordingly.
  - Updated `README.md` and `example-config.toml` to document that `build-image` attaches `cluster.lustre_network` when configured.
