- [x] After working around Ceph credentials and using Pulumi CLI 3.226.0, `hailstack create --dry-run` reaches OpenStack preflight but fails before preview with `NetworkError: OpenStack CLI returned a non-object JSON payload`. The failing command is `openstack limits show --absolute -f json` from OpenStackClient 10.0.0. On the user's machine it returns a JSON list of rows like `[ {"Name": "floating_ips", "Value": 10}, {"Name": "instances", "Value": 482}, {"Name": "instances_used", "Value": 312}, {"Name": "max_total_cores", "Value": 2413}, ... ]`, not a JSON object. Hailstack should accept this OpenStackClient 10 limits shape and correctly compute compute quotas instead of failing.
  - Touched `src/hailstack/cli/commands/create.py` and `tests/cli/commands/test_create.py`.
  - Added a regression test for OpenStackClient 10 absolute-limit `Name`/`Value` row JSON using the observed remote compute quota fields.
  - Updated OpenStack CLI JSON parsing to accept valid name/value row lists and compute quotas from both legacy camelCase fields and OpenStackClient 10 snake_case limit fields without weakening missing or invalid integer checks.
