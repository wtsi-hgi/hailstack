- [x] A freshly created cluster reaches `cluster ready`, but the master is not usable because `jupyter-lab.service` fails immediately with `TypeError: create.<locals>.Validator.__init__() got an unexpected keyword argument 'registry'`. The built image contains `jupyterlab 4.5.8` with `jsonschema 3.2.0`; Hailstack should bake a compatible Jupyter dependency set so JupyterLab starts without manual package repair.
  - Pinned the image's Jupyter stack to versions compatible with gnomAD's `jsonschema<4` constraint and made `jupyter.sh` validate the result with `pip check`, Jupyter server imports, and version checks.
  - Moved Packer provisioner order so `gnomad.sh` runs before `jupyter.sh`, letting the Jupyter repair happen after gnomAD installs its constrained dependencies.
  - Added regression coverage for repairing the observed bad `jupyterlab 4.5.8` / `jsonschema 3.2.0` state and for preserving the gnomAD-before-Jupyter provisioner order.
  - Implementor and reviewer ran `ruff`, `pyright`, targeted Packer/Jupyter tests, and the full pytest suite; full suite passed with `371 passed, 8 skipped`.
