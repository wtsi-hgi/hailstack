- [x] A freshly created cluster lets the user SSH to the master with the runner's default `~/.ssh/id_rsa`, but direct SSH to worker nodes such as `ubuntu@192.168.252.142` fails with `Permission denied (publickey)`. The worker console shows cloud-init installed configured public keys that do not match the runner's default key, so Hailstack should include the runner's default SSH public key in node authorized keys automatically when possible, or otherwise fail early with clear configuration guidance, so users can manually SSH to all created instances later.
  - Added create-time SSH key enrichment that appends readable default OpenSSH public key files from the runner after configured keys, preserving configured key order and deduplicating without reading private keys.
  - Kept the first configured key as the OpenStack keypair source while passing the effective key list into Pulumi so master and worker cloud-config receive the same early authorized keys.
  - Added regressions for schema enrichment, missing default-public-key failure guidance, create-command propagation, and master/worker cloud-init rendering.
  - Updated README guidance to describe default `.pub` discovery and the no-private-key guarantee.
  - Implementor and reviewer ran `ruff`, `pyright`, targeted SSH-key tests, and the full pytest suite; full suite passed with `376 passed, 8 skipped`.
