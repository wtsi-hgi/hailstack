- [x] Would this be an intended and reasonable way of doing things? If so, improve the documentation so users know how to actually use the clusters they create. If usage could be better, use the bugfix skill workflow to improve the usage and then update the docs.
  - Added a baked `/etc/profile.d/hailstack.sh` login-shell runtime profile that exports the Hailstack base/overlay venv paths, Spark/Hadoop homes, runtime Python, and PySpark Python.
  - Ordered PATH so `python` resolves to the overlay venv, Spark/Hadoop commands resolve to `/opt/spark` and `/opt/hadoop`, and baked base-venv console scripts such as `jupyter` remain available.
  - Documented cluster usage after `create`: Jupyter URL/auth, SSH runtime commands, Hail/gnomAD imports, Spark/Hadoop commands, and where to install repeatable Python additions.
  - Added regression coverage for the login runtime profile and README usage docs.
  - Implementor and reviewer ran `ruff`, `pyright`, targeted runtime/docs tests, and the full pytest suite; full suite passed with `393 passed, 8 skipped`.
