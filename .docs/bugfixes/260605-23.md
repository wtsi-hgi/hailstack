- [x] After the keypair fix, real `hailstack create` gets past keypair, security groups, rules, and management ports, but every Lustre port fails with Neutron 400 `PortSecurityAndIPRequiredForSecurityGroups`: `Port security must be enabled and port must have an IP address in order to use security groups.` The Pulumi resource graph creates Lustre network ports using `_create_port(..., [master_security_group.id] / [worker_security_group.id], tags)`, attaching security groups to the Lustre network. Earlier Packer bugfixes showed this OpenStack project's Lustre network must not receive security groups/port-security-specific settings. Cluster Lustre ports should be created without security groups while management/cloudforms ports keep their security groups.
  - Updated `src/hailstack/pulumi/resources.py` so Lustre master/worker ports use `no_security_groups=True` and omit `security_group_ids`, while management ports still attach the master/worker security groups and all port tags remain unchanged.
  - Added `tests/pulumi/test_resources.py::test_lustre_ports_omit_security_groups_and_port_security_override`, which failed before the implementation because Lustre ports carried security-group IDs and now asserts they omit `security_group_ids` and `port_security_enabled`.
  - Ran `timeout 180 uv run pytest tests/ -v -k test_lustre_ports_omit_security_groups_and_port_security_override` before and after the fix, `timeout 120 uv run ruff check src/ tests/`, `timeout 120 uv run ruff format src/ tests/`, `timeout 180 uv run pyright`, `timeout 180 uv run pytest tests/pulumi/test_resources.py tests/integration/test_pulumi_lifecycle.py -v`, and `timeout 600 uv run pytest tests/ -v`.
