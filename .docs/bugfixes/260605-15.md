- [x] hailstack build-image now reaches ubuntu/hadoop.sh after package provisioning succeeds and Scala 2.12.18 is installed, but hadoop.sh exits with status 1 because Hadoop reports: ERROR: JAVA_HOME is not set and could not be found. The image build should configure Java so Hadoop provisioning can run non-interactively after OpenJDK is installed.
  - Added a hermetic regression for `packer/scripts/ubuntu/hadoop.sh` that runs without `JAVA_HOME`, exposes only an OpenJDK-style symlinked Java binary on `PATH`, and requires Hadoop's version check to see the discovered Java home.
  - Updated `hadoop.sh` to validate an existing `JAVA_HOME`, otherwise discover it from `javac` or `java`, export it for provisioning, and persist it to Hadoop's `hadoop-env.sh` for later Hadoop commands.
  - Preserved the existing sudo-provisioned Packer script flow, apt-lock behavior, Packer networking/logging cleanup coverage, and Scala 2.12.18 package provisioning regression.
  - Ran `timeout 120 uv run ruff check src/ tests/`, `timeout 120 uv run ruff format src/ tests/`, `timeout 180 uv run pyright`, `timeout 180 uv run pytest tests/packer/test_builder.py tests/integration/test_packer_scripts.py -v`, `timeout 180 uv run pytest tests/test_developer_docs.py -v`, and `timeout 600 uv run pytest tests/ -v`.
  - Local Packer CLI remains unavailable, so `test_e2_packer_template_validates_with_packer_cli` skipped with `packer CLI not installed`; the full suite passed with existing Pulumi OpenStack deprecation warnings and a pytest unraisable event-loop warning.
