- [x] Remote build-image now installs the configured Scala 2.12.18 and completes ubuntu/packages.sh, but ubuntu/hadoop.sh fails with ERROR: JAVA_HOME is not set and could not be found. Hadoop provisioning should set or discover JAVA_HOME from the installed Java version so build-image continues without user involvement.
  - Added a hermetic regression for `packer/scripts/ubuntu/hadoop.sh` that runs without `JAVA_HOME`, exposes only an installed Java binary on `PATH`, and requires Hadoop's version check to see the discovered `JAVA_HOME`.
  - Updated `hadoop.sh` to validate an existing `JAVA_HOME`, otherwise discover it from `javac` or `java`, export it for provisioning, and persist it to Hadoop's `hadoop-env.sh` for later Hadoop commands.
  - Ran `timeout 120 uv run ruff check src/ tests/`, `timeout 120 uv run ruff format src/ tests/`, `timeout 180 uv run pyright`, `timeout 180 uv run pytest tests/packer/test_builder.py tests/integration/test_packer_scripts.py -v`, `timeout 180 uv run pytest tests/test_developer_docs.py -v`, and `timeout 600 uv run pytest tests/ -v`.
  - Also ran `timeout 120 uv run ruff check src/ tests/` after formatting and `timeout 60 bash -n packer/scripts/ubuntu/hadoop.sh`; local Packer CLI remains unavailable, so `test_e2_packer_template_validates_with_packer_cli` skipped with `packer CLI not installed`.
