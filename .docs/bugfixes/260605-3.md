- [x] Routing/correct network usage is unclear and `build-image` can launch a Packer instance with only a private 192.x address even when working clusters use the configured networks/floating IP setup; Hailstack should use configuration better so image builds select reachable networking without requiring OpenStack/networking expertise.
  - `build-image` now uses `cluster.floating_ip_pool` when `[packer].floating_ip_pool` is blank, while preserving `[packer].floating_ip_pool` as an explicit image-build override.
  - Kept `cluster.network_name` as the Packer management network and added pre-Packer logs for the selected network and floating IP pool mode.
  - Updated README and `example-config.toml` to explain runner-routable build networking, the cluster-level default, and the packer-specific override.
