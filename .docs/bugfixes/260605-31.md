- [x] A cluster created from rebuilt image `19d4a6df-7435-4734-991a-c1629506659e` reaches `cluster ready` and all nodes are ACTIVE on both `cloudforms_network` and `lustre-hgi01`, but cloud-init never reaches final setup because the baked `mountLustre.service` runs `/usr/local/sbin/mountLustre start` against the image's preexisting `/etc/fstab` entries. The user's config has `cluster.lustre_mount_target = "10.160.42.101@tcp3:10.160.42.100@tcp3:/lus26"`, yet the live master `/etc/fstab` still contains multiple baked entries for `/lus24`, `/lus25`, `/lus26`, and `/lus27`, and `mountLustre` is currently pinging unrelated targets such as `lus22-mds1@tcp3`. Hailstack should make boot/final setup use only the configured Lustre mount target, or otherwise not let baked site-wide Lustre mount attempts block cloud-init, so `create` produces a usable cluster with SSH, Jupyter, Spark History, and the configured Lustre mount.
  - Added cloud-init regressions proving master and worker first-boot payloads stop/mask baked `mountLustre.service`, remove stale Lustre `/etc/fstab` rows, and append only the configured `cluster.lustre_mount_target`.
  - Cloud-init now runs that cleanup in early `bootcmd`, before cloud-final can be blocked by baked site-wide Lustre mounts, and repeats the cleanup immediately before Hailstack's configured `/lustre` mount.
  - No-Lustre configurations do not append a bogus Lustre mount.
  - Implementor and reviewer ran `ruff`, `pyright`, targeted cloud-init tests, and the full pytest suite; full suite passed with `381 passed, 8 skipped`.
