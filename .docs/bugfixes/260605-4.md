- [x] `PACKER_LOG=1 PACKER_LOG_PATH=packer-debug.log hailstack build-image ...` writes `packer-debug.log` inside the installed Packer data directory because Hailstack runs Packer with `cwd` set there; relative user-supplied log paths should resolve from the user's current directory instead.
  - Added a regression test covering an installed-style Packer data directory, separate caller cwd, and relative `PACKER_LOG_PATH`.
  - Normalized relative `PACKER_LOG_PATH` values to absolute paths from the caller cwd for the Packer runner only, while preserving absolute paths and the existing template-directory runner cwd.
